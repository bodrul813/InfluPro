
namespace InfluPro.DAL.Enumarations
{
    public enum Status
    {
        Active = 1,   // Represents an active status
        Inactive = 0,  // Represents an inactive status
        Blocked = 2,  // Represents a blocked status
        Deleted = 3   // Represents a deleted status
    }
}
